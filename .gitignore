.venv/
out/
runs/
__pycache__/
*.pyc
.DS_Store
ui/node_modules/

# The exercise brief is stamped "Confidential" in its own header, and this
# repository is public. The two sample documents are kept because they are
# marked fictional and the test suite reads them directly.
practical-exercise-daniel-protocolqc.pdf

# Local drafting, not part of the deliverable
EMAIL-DRAFT.md

# API keys. The tool reads NVIDIA_API_KEY from here when it is not in the
# environment; .env.example is the committed template and holds no key.
.env
.env.local

# Interview prep, not part of the deliverable
PRESENTATION.md
